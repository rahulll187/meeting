import React, { Component } from 'react';
import { View, Text } from 'react-native';

export default class AddEventsScreen extends Component {
  render() {
    return (
      <View style={{ alignItems: 'center' }}>
        <Text>AddEvents Screen</Text>
      </View>
    );
  }
}
